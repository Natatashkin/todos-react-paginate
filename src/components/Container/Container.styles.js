import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => {
  return {
    container: {
      maxWidth: '800px',
      marginLeft: 'auto',
      marginRight: 'auto',
    },
  };
});
