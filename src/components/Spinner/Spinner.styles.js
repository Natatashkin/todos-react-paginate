import makeStyles from "@mui/styles/makeStyles";

export const useStyles = makeStyles(()=> {
  return{
    spinner: {
      display: 'flex',
      width: '100%',
      justifyContent: 'center',
      alignItems: 'center',
    }
  }
})